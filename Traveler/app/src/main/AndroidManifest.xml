<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="math.uni.lodz.pl.jacek.traveler">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity
            android:name=".AddAttractionActivity"
            android:screenOrientation="portrait" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".SynchronizationActivity" />
        <activity android:name=".BrowseAttractionActivity" />
        <activity android:name=".AttractionDetailsActivity" />
        <activity android:name=".BrowseTourActivity" />
        <activity android:name=".TourDetailsActivity" />
        <activity android:name=".TourAttractionsMapActivity" />
        <activity android:name=".AddTourActivity" />
        <activity android:name=".PickAttractionsForTourActivity" />
        <activity android:name=".ShowTourAttractionsActivity" />
        <activity android:name=".TripActivity" />
        <activity android:name=".TripAttractionsActivity" />
        <activity android:name=".PreviewTripActivity" />
        <activity android:name=".MyTripsActivity" />
        <activity android:name=".ShowMyTripActivity" />
        <activity android:name=".DownloadOrRemovePhotosActivity"></activity>
    </application>

</manifest>